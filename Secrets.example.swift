import Foundation

// Setup: copy this file to BOTH locations and fill in your values:
//   • MFT Hub/Secrets.swift
//   • MFTWidget/Secrets.swift
// (Both are git-ignored. The widget is a separate target, so it needs its own copy.)
enum Secrets {
    static let serverURL = "https://your-server.example.com/mftserver"
    static let token = "your-shared-token"
}
